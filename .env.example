# ==========================================
# Badminton Forum 環境變數設定範例
# ==========================================
# 請複製此檔案為 .env 並修改為你的實際設定值
# cp .env.example .env

# ==========================================
# 資料庫設定 (MariaDB)
# ==========================================
MARIADB_DATABASE=badmintonforumdb
MARIADB_USER=badmintonuser
# 請更改為安全的密碼 (生產環境必須更改!)
MARIADB_PASSWORD=CHANGE_THIS_PASSWORD
MARIADB_ROOT_PASSWORD=CHANGE_THIS_ROOT_PASSWORD
DB_PORT=3306

# ==========================================
# API 設定
# ==========================================
API_PORT=5246

# JWT 加密金鑰 (至少 32 字元)
# 生產環境請使用強密碼，可用以下指令產生：
# openssl rand -base64 32
JWT_SECRET=CHANGE_THIS_TO_A_VERY_SECRET_KEY_AT_LEAST_32_CHARACTERS

JWT_ISSUER=BadmintonForumAPI
JWT_AUDIENCE=BadmintonForumClient
JWT_EXPIRATION_DAYS=7

# ==========================================
# 前端設定
# ==========================================
WEB_PORT=5173
VITE_API_URL=http://localhost:5246/api

# ==========================================
# Google OAuth 設定 (選用功能)
# ==========================================
# 如果要啟用 Google 登入功能，請：
# 1. 前往 https://console.cloud.google.com/
# 2. 建立專案並啟用 Google Identity API
# 3. 設定授權來源為 http://localhost:5173 (開發) 或你的網域 (生產)
# 4. 將 Client ID 填入下方
# 詳細步驟請參考 docs/GOOGLE_OAUTH_SETUP.md
#
# 如果不需要 Google 登入，可以保持預設值或註解掉
GOOGLE_CLIENT_ID=YOUR_GOOGLE_CLIENT_ID.apps.googleusercontent.com

# ==========================================
# Adminer 資料庫管理介面 (選用)
# ==========================================
# 訪問 http://localhost:8080 使用 Adminer
# 使用 docker-compose --profile tools up 來啟用
ADMINER_PORT=8080

# ==========================================
# 環境模式設定 (選用)
# ==========================================
# 取消註解以下設定來切換到生產環境模式
# NODE_ENV=production
# ASPNETCORE_ENVIRONMENT=Production

# ==========================================
# Email 服務設定 (選用)
# ==========================================
# 開發環境預設使用 Console Email (輸出到 log)
# 生產環境需要設定 SMTP
# EMAIL_FROM=noreply@yourdomain.com
# EMAIL_SMTP_HOST=smtp.gmail.com
# EMAIL_SMTP_PORT=587
# EMAIL_SMTP_USERNAME=your-email@gmail.com
# EMAIL_SMTP_PASSWORD=your-app-password